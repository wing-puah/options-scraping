# Minimal deps for the Compile Flow workflow (scripts/compile_flow.py + scripts/gc_flow.py).
# These scripts only touch Drive + pandas — no Playwright/scipy/yfinance/openai/gspread —
# so the daily compile job installs this slim set instead of the full requirements.txt.
# Keep versions in sync with requirements.txt.
python-dotenv>=1.0
google-api-python-client>=2.100
google-auth>=2.29
pandas>=2.0
