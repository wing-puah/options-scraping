New volume, shaped like archive/20: title line, `_Status: historical (covers 2026-09-09). Conclusions stand as of 2026-09-17. Live record: [current.md](../current.md)._`, a "Covers 2026-09-09." summary drawn only from the moved text (107 journalled rows, 55 mapped over 20 signal dates, no tier-A close; live_loop retirement, 159/0 reconcile diff, 3527 tests), then the two sections from current.md:222-317 copied verbatim.
Three internal links in the moved body were repointed for the new directory depth (research/ -> research/archive/): `next-steps.md#s2-5` -> `../next-steps.md#s2-5` (twice) and `../docs/deployment-rules.md#s1` -> `../../docs/deployment-rules.md#s1`. All other text, including both `##` headings, is byte-identical to current.md's committed lines 222-317 (verified: `sed -n '222,317p' HEAD` vs this file's body, diff only on the three link targets).
Checks: `headings_kept` (research-headings) passes trivially — baseline for a brand-new file has no headings to lose. `archive-status-only` FAILS: its script requires a pre-existing status line in the baseline, which doesn't exist for a file this proposal creates; this is a property of the check (it assumes an existing volume), not a defect in the new content — the status line here is present, correctly formatted, and first in the file.
