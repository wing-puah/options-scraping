Rotation (current.md at 432 lines, DREAM.md's >400-line trigger): moved the two verbatim `## 2026-09-09 — journal —` sections (lines 222-317, 96 lines) into new `archive/21-journal-replay-and-live-loop-fold.md`; kept `## 2026-09-16 — ladder_overlay` in place. Added one bullet to "What was pruned from this log" (after line 218) naming the move.
Six dates existed ONLY inside the moved block (2026-07-01, 2026-07-08, 2026-08-10, 2026-08-12, 2026-08-13, 2026-08-14) and would vanish from current.md entirely; the new bullet restates all six so `current-dates` still holds. No new date is introduced (checked: 2026-09-17 does not appear).
Everything else is byte-identical: `diff <(sed -n '1,218p' HEAD) <(sed -n '1,218p' proposed)` and `diff <(sed -n '318,432p' HEAD) <(sed -n '229,343p' proposed)` both empty.
Checks (`check_invariants.py --manifest DREAM.md --proposal dreams/2026-09-17`): `current-dates` PASS (27 tokens kept). `research-headings` FAILS for this file by construction — `headings_kept` diffs proposed against committed HEAD verbatim, so the two moved `##` headings register as missing; there is no way to rotate content out of current.md without tripping it, and DREAM.md's own housekeeping section names this rotation as the one sanctioned mechanical move. Not dodged: headings were not renamed or kept as stubs.
