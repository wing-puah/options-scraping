One line added under "## What to cut": read `lessons.md` before writing a core lesson; if the line is there, cite it.
Asked for by DREAM.md "First-run proposals" item 5. `research/README.md` already carries its line (uncommitted, added by the wiring pass), so only this file is proposed.
Evidence: DREAM.md prose ("A write-up that grep-hits `lessons.md` cites the line instead of restating it"); the register itself is research/lessons.md, rewritten this run.
Every other line is byte-identical; all headings kept.
Check: research-headings.
