# Study map — what each file in `scripts/backtest_study/` is trying to prove

> **There is a rendered version of this page**: [`site/study-map.html`](../site/study-map.html).
> Open it in a browser. It carries everything below *plus* what each study's
> last run actually printed, quoted out of `backtests/study_output/`, and the
> newest sections of [`current.md`](current.md). It rebuilds itself after every
> `python -m scripts.backtest_study run …` and every `make study-review`; to
> rebuild by hand, `make study-map` (or `make study-map-open`).
>
> Its per-study verdicts come from `scripts/study_map/catalog.py`, which is the
> file to edit when a verdict changes. A study whose entry is missing there
> fails the test suite. This markdown is the prose companion; keep the two
> saying the same thing.

One page, read top to bottom. `scripts/backtest/` **prices** the book;
`scripts/backtest_study/` **argues** about it. Nothing here is imported by
production, nothing runs on a schedule, and no study writes config. A study
ends in a plain-text report that becomes an addendum in [`current.md`](current.md),
and a human decides whether anything ships.

The four sections below are the same order the studies sit in on disk:
`f1_selection/` → `f2_management/` → `f3_structure/` → `f4_deployment/`
("pick it, manage it, wrap it, fund it"). The Infrastructure table at the
bottom covers `run.py`, `harness.py`, `book.py`, `protocol.py`, the `underlying*`
and `volume_features` families. It lives in `lib/`: import-only, and it argues
nothing, so it carries no verdict and no family of its own.

---

## The shape of the whole thing

```
                    ANALYSIS ENGINE  (v3 frozen · v4 live)
                              │  emits plays
                              ▼
        ┌─────────────────────────────────────────────────┐
        │  THE BOOK          book.py                      │
        │  real rows (BacktestResults) + proxy rows        │
        │  (BacktestProxy, strike/expiry tweak only)      │
        │  calibration gate: a row that doesn't replay     │
        │  to its stored outcome does not get in           │
        │                                                  │
        │  n is the ~118 SIGNAL DATES, not the ~1,100 rows │
        └─────────────────────────┬───────────────────────┘
                                  │  every study replays through
                                  ▼
        ┌─────────────────────────────────────────────────┐
        │  harness.py    FROZEN exit-replay engine         │
        │                (DO NOT EDIT — every recorded     │
        │                 conclusion rests on it)          │
        │  protocol.py   purged walk-forward · date-       │
        │                clustered CIs · LOO · window cuts │
        │  underlying*.py stock bars + as-of-entry price   │
        │                 features (no look-ahead)         │
        └─────────────────────────┬───────────────────────┘
                                  │
        ┌───────────────┬─────────┴────────┬───────────────┐
        ▼               ▼                  ▼               ▼
  ① SELECTION     ② MANAGEMENT       ③ STRUCTURE     ④ DEPLOYMENT
  what to trade   when to get out    what wrapper    can I run it
        │               │                  │               │
        │               │                  │               │
   mostly NULL     where the edge      one +0.085     $25k account
   0/496 subsets   actually lives      that dies      caps survive
   0/15 ML cells   (2 rules shipped)   out of sample  window doesn't
        │               │                  │               │
        └───────────────┴────────┬─────────┴───────────────┘
                                 ▼
                    docs/deployment-rules.md
                    the operator card — top 3/day, tiers A/B
```

**The one-sentence result of the whole programme:** selection is not tunable
from the columns we have; the money is in the exit rules and in position
management. Every selection study returns a null; two exit studies shipped.

---

<a id="selection"></a>
## ① SELECTION — "which plays are worth taking?"

| File | The question | Verdict |
|---|---|---|
| [`regime_gap_reread.py`](study-results/f1_selection/regime_gap_reread.md) | Numbers only, no interpretation: build the pooled book and print the report. | Baseline snapshot other studies import verbatim — no verdict line, by design. The one carry-forward from the 2026-09-04 v4 run (1,143 rows / 166 dates) is [§5d](../docs/deployment-rules.md#s5), the Tier-C continuity check: `iv_spread vs mae_pct \| bear_put_spread, pooled          n=  365  rho=-0.0686  p= 0.1912` against v3's −0.215 at p<.0001 on n=380 — the non-replication confirmed at COMPARABLE n, not excused as power. |
| [`mech_regime_recut.py`](study-results/f1_selection/mech_regime_recut.md) | Does a *deterministic* regime label (pure function of SPY/VIX history at the signal date) beat the model's free-text regime? | Overlay adopted — [`mech_cell`](glossary.md#mech_cell) is a column now and it keys the shipped BEAR_HE exit. The OR-veto extension stays rejected on 2026-09-04 (v4, 1,143 rows / 166 dates): `VERDICT RULE: OR-VETO REJECTED (newly-vetoed subset net-positive)`, the newly-cut rows being net POSITIVE at `n=   41  mean=  0.0526  total=   2.1550` — which relieves the 08-27 read's net-flat caveat. Read past the hardcoded Mar-2026 table: it prints all zeros because this export's 2026 dates are January, February and April. |
| [`bear_position_study.py`](study-results/f1_selection/bear_position_study.md) | Pre-registered cuts on `bear_put`: is it a **selection** problem ([E](glossary.md#e)<0) or an **exit** problem (E>0, [R](glossary.md#r)<0)? | DEMOTE criteria all fire, now at n=368 (2026-09-04, v4): `ex-window mean E < 0            (-0.222)`, [CI](glossary.md#ci) `[-0.349, -0.087]`, halves `early -0.388, late -0.045`. Note what they are on: **E**, the exit-free number. On **R** the same 368 rows no longer separate (`mean= -0.087  95% CI [-0.197, +0.026]   CI spans 0`) — a selection verdict, not an exit one. |
| [`bear_arm.py`](arm-index.md#bear_arm) | B1 — is there *any* bear subset defined at decision time that isn't negative? B2 — is the exit just mis-tuned? | **B1 NO** (0 survivors of 496 subsets, 194 with n≥40, ~9.7 expected false). **B2**: `be_after: 0.50` shipped 2026-08-11 and REVERTED 08-24 — its rollback census is now on its third export and its third answer (08-24 FIRED 92 rows/53 dates, 08-27 HOLD 165/96, 09-04 FIRES again at `n_rows=199  n_dates=110  floor=60 rows  -> FLOOR MET` on the new 2026 column, `2026:-0.0431`), and nothing changes operationally because `structure_exit.enabled` is already false. Separately, B2's pre-registered EXIT FIX criteria are **MET for the first time** (`sl .50 (tighter)  Δ=+0.039 CI[+0.004, +0.071] LOO min gain +0.035`, bear-specificity control holding) — a FIRST CLEAR on the correlated-window re-read, which holds the rule and promotes nothing. |
| [`ml_combination.py`](arm-index.md#ml_combination) | Does any learned combination (structure × regime × geometry × enrichment) beat the score-free ladder out of sample? | **NULL** — `VERDICT: NULL RESULT — the ladder is at/near the ceiling of this data`; M3 −0.045 CI95 [−0.160, +0.074] on 2026-09-04. The registration's positive-in-≥2-of-3-years clause is a REAL three-year test for the first time (`2024:+0.300  2025:+0.070  2026:-0.251`), passing on the two old years and failing on the new one. Re-open only on new **columns**, never new models. |
| [`v4_bridge.py`](study-results/f1_selection/v4_bridge.md) | v4 dropped two prompt factors. Does the v3-derived ladder still apply to what v4 *emits*? | **LADDER UNVALIDATED ON v4**, and on 2026-09-04 ALL FIVE pre-registered tests shift: credit share, the last one holding at `z = -1.43` on 08-24, now reads `two-proportion z = -2.10, p = 0.0355   *** SHIFT`. `1465 plays / 142 dates` of v3 against `2025 plays / 184 dates` of v4; tier mix A 15.4% → 11.1%, B 13.7% → 26.8%, C 60.8% → 61.0%, VETO 10.2% → 1.1% at `chi2 = 222.54`. Keep deploying under the v3 rules; do not re-derive the ladder on v4 rows. |
| [`text_features.py`](arm-index.md#text_features) | Does the model's own prose (invalidation, trigger, specificity, thesis/alt shape, blind-labelled thesis type and confidence, citation faithfulness) separate outcome within structure × tier, or raise mean R and [PF](glossary.md#pf) as a gate on the shipped ladder? | First run 2026-09-02 (era v4 PRIMARY, labels 100%, citations 148/148 dates): **every feature NULL or UNDERPOWERED in all three arms** — `PROMPT-ROBUSTNESS FINDINGS ... none`, `ENTRY-GATE CANDIDATES ... none`. Only 1.6% of cited flow figures are unmatched in the raw feed. v3 SECONDARY the same, with one `alt_ratio` veto that clears the conjunction but lowers mean R (caught by the catch-all). The 2026-09-04 re-run (1,143 priced rows / 166 dates) says the same at the top, with one caveat: the label cache does not cover the new rows and nothing re-labelled them (`claude calls=0`), so `text_features ARM B` coverage fell to `1021/1143 priced rows (89.3%)` with 122 UNLABELLED — no ARM B line is quotable at this population until a live-label re-run. |
| [`prompt_eval.py`](arm-index.md#prompt_eval) | The loop step: does a CANDIDATE prompt beat the shipped one on the same dates under the shipped ladder (paired ΔR, PF, hallucination rate, zero bear_call leaks), with accumulated live dates as the primary evidence? | Harness built 2026-09-02; date sets declared by rule; PROD noise floor is the first scored step. No candidate scored yet — scoring needs a committed candidate directory. MET is a v5-bump proposal, never a ship. Read the `-latest.txt` on disk knowing what it is: `prompt_eval` takes a subcommand, so the bulk `run --all` invocation is a DESIGNED refusal (`error: the following arguments are required: cmd`, exit 2), not a failed study; the scored run of record is the PROD variance floor. |
| [`macro_event_study.py`](arm-index.md#macro_event_study) | Do scheduled macro events (FOMC decisions, minutes, CPI, NFP, PCE) show up in the book — in entry IV (`vrp`), in outcomes (R/E), or in exits? Distance keys off the **entry session**, pre-open vs post-open decides day 0. | Era v3 (795/118): ONE powered cell — NFP AFTER w≤5 — null on `vrp` and R; every FOMC/minutes/CPI/PCE cell underpowered. ARM X's raw trigger **died under the survival control** → `macro_event_exit` DE-QUEUED. The 2026-09-04 v4 re-run (1,143 rows / 166 dates) lands in the same place from a book twice the size: `X-C1 verdict (164 affected dates vs floor 25): SURVIVAL-ARTIFACT`, with the census the thing that grew — `hold spans >=1 macro event: 1034 rows / 166 dates  mean R +0.059  mean days_held 36.0`. The export's first 2026 dates put two starred `iv_spread` cells on the board at 10–12 rows over 1–2 dates; not readable, and nothing is quoted from them. Nothing ships; no v5 bump. |
| [`trigger_entry.py`](arm-index.md#trigger_entry) | Production enters every non-vetoed play unconditionally at the next open and ignores the stated trigger. Does entering only WHEN that trigger level is first crossed — at the crossing session's CLOSE, contracts re-sized, the whole trade re-priced through the frozen harness — beat it once the entry price PAYS for the confirmation? [`exit_from_text`](arm-index.md#exit_from_text) E2 censused the selection but kept the next-open fill, so the favourable move sat inside its ENTERED number. | First run 2026-09-04 (era v4 PRIMARY), re-run the same evening on the refreshed export (1,143 rows / 166 dates; `35 HARD  of 1143` at admission): **`tally: {'NULL': 1, 'LATE-ENTRY': 2}`** — no CANDIDATE. E2's selection census reproduces at shipped pricing (`N=3      651 rows/162d +0.178    301 rows/136d -0.053`) and the re-pricing eats it (`ARM T  N=3 ... n=645 rows / 162 dates ... DeltaR -0.0121`, `N=5 ... -0.0232`, CIs spanning zero). ARM C localises it: waiting only helps rows the day-0 mark had already marked DOWN (`day-0 P&L <= -25%          n=  61  DeltaR +0.6297` vs `day-0 P&L > +25%           n=  56  DeltaR -0.4151`) — [`next_day_move`](arm-index.md#next_day_move)'s confound, not a text finding. The N grid flips sign, so criterion 7 fails everywhere; ARM D is flat on the shipped card. v3 SECONDARY: `tally: {'LATE-ENTRY': 3}`. **The trigger picks a better book and the confirmation costs what it is worth.** |
| [`emission_timing.py`](arm-index.md#emission_timing) | Same signal, later entry: do repeat emissions of a persisting (ticker, structure) underperform first emissions, and does a fill delayed 1–3 sessions past the signal decay the edge — conditioned on the pre-signal price vector? | First run 2026-08-19 (era v3): `emission_timing ARM P` **NULL** (+0.054, CI spans zero); `emission_timing ARM L` **LAG-TOLERANT** — the publishable finding: no lag in {1,2,3} sessions separates from the day-0 close fill. Both headlines survive the 2026-09-04 v4 run (1,143 rows / 166 dates), which is the first whose `ex_2026_feb_apr` cut is not a no-op — and the `**` candidates collapse from three to ONE: ARM L's `T1_low L=3 vs L=0` (`mean delta -0.1330   CI[-0.2046,-0.0588]`), clearing all six criteria including 2026. The two ARM P sub-cuts that used to clear now FAIL criterion 4 on a 2026 SIGN FLIP (`consecutive repeats` `2026 +0.2948`; `ohlc: repeats that moved AGAINST the play` `2026 +0.5415`). The 08-24 review's OFF-BASIS retraction stands; no intake rule is proposed. |

<a id="management"></a>
## ② MANAGEMENT — "when do I get out?" ← where the edge is

| File | The question | Verdict |
|---|---|---|
| [`exit_mechanism_study.py`](study-results/f2_management/exit_mechanism_study.md) | The original grid: replay stored daily marks under alternative exit rules, real rows only. | **SHIPPED** the production debit profile — pt 0.90 / sl 0.75 / tef 0.75, no trail (Attempt 10). On the 2026-09-04 v4 export (408 debit rows, `→ 392 exact, 1 near-rounding-tie, 14 superseded-basis, 1 boundary-tie, 0 HARD of 408`) PROD reads `total=$   +25363` — the `$-959` in the older record was the then 280-row book, a different population. Nothing ships from the grid: the best out-of-fold cells are still target/ratchet (`BE ratchet @.75, no trail` `Δ-LOO=$   +2142`, `pt 1.10 no trail` `Δ-LOO=$    +475`), and the news is only that a REACTIVE cell finally prints a positive Δ-LOO at all (`trail .40 trig .75` at `+200`) beside the grid's worst, `trail .25 trig .50` at `Δ-LOO=$  -12642`. Credit arm: the Attempt-13 rollback census is `fresh bull_put rows (signal_date > 2026-07-13): 0` against a floor of 15 → UNDERPOWERED and structurally unreachable until signal dates after 2026-07-13 exist (this book ends 2026-04-16); the comparator still favours sl-none but the margin collapsed to `Δ=$    -584  Δ-LOO=$   -1306`. |
| `combined_exit_study.py` — **DELETED** | Same grid, bigger tuning set (real + proxy-priced rows pooled). | **RETIRED 2026-08-14, module DELETED 2026-09-05.** Its inputs (`backtests/results_proxy.csv`, an author transposition that never matched `config/backtest.yml`'s actual `proxy_results.csv` name) were gitignored scratch, deleted and unrecoverable, so it could not be re-run. **This row is now the record.** Verdict, reached while its inputs existed and unchanged by the deletion: **NOTHING SHIPPED** — a `reference` study, it confirmed the exit profile ALREADY in production was the best *global* config, so there was nothing to change. Its one consequence was a follow-up question: the same run showed exits are regime-conditional, which is what motivated the two switch studies below (the BEAR_HE trail shipped from those, not from this). Full trail: [archive/02](archive/02-credit-debit-split-attempts-8-12.md) (Attempts 8, 9, 12). Nothing re-opens this. |
| `underlying_exit_study.py` — **DELETED** | Credit spreads: stop on the **underlying** breaching a level instead of on the mark? | **RETIRED 2026-08-14, module DELETED 2026-09-05.** Its second input (`backtests/v2_BacktestResults_nocreditdiff.csv`) was gitignored scratch, deleted and unrecoverable; the genuine rename `v2_results_nocreditdiff.csv` survives but holds 0 credit rows, so re-pointing would only emit an empty report. **This row is now the record.** Verdict, unchanged by the deletion: **NOTHING SHIPPED** — a `null`. Stopping on the underlying did not beat stopping on the mark (❌ Attempt 9). Full trail: [archive/02](archive/02-credit-debit-split-attempts-8-12.md). Nothing re-opens this. |
| [`exit_switch_mech_study.py`](study-results/f2_management/exit_switch_mech_study.md) | Per-regime exit switch keyed on the mechanical regime — stable where the model-keyed version failed [LOO](glossary.md#loo)? | **BEAR_HE cell SHIPPED** (trail 0.50 / trigger 0.50). On 2026-09-04 BOTH verdicts STAY GATED. BEAR_HE's own rollback census still has no reading (`n_rows=1  n_dates=1  floor=25 dates  -> UNDERPOWERED`); LVOL is four times past its floor (`n_rows=100  n_dates=73`) and now FAILS 2 of the 4 corrected criteria — `median=-0.0330  total=+9.8643  (n=73 affected dates)` with halves `early Δ=+10.0657  late Δ=-0.2015` — so `VERDICT: LVOL (tef null) STAYS GATED.` and the 08-24 CLEARED read (which the operator HELD anyway) is superseded, not re-argued. L-VOL and RANGE/BULL stay commented out. |
| [`exit_switch_structure_study.py`](study-results/f2_management/exit_switch_structure_study.md) | Q1: does a bear_put-keyed trail pass the same ship gate? Q2: is BEAR_HE secretly just a *composition proxy* for that structure effect? | Guards the shipped rule against the composition trap that killed `oi_confirm_pct`. Q1 still `STAYS GATED` on 2026-09-04, failing 2 of 6 (LOO median, both time halves). Q2 **INVERTED**: the structure-keyed trail now reads `Δ=+4.6539   on its complement (outside BEAR_HE) Δ=+3.8804   retained 83%` where 08-24 read −0.8920 / −1.6656 / 187%, so the complement criterion PASSES — read that as the trap the guard exists for (a key that pays 83% of its delta outside its own cell is not a key), not as support. The shipped clause is identical: `Δ=+0.7735   on its complement (non-bear_put) Δ=+0.0000   retained 0%`. |
| [`bear_giveback.py`](arm-index.md#bear_giveback) | 82% of bear rows go green then give it back. Can a peak-triggered breakeven stop capture it, and does the underlying path explain it? | `be_after` grid does **NOT** ship beyond what is already live — and nothing is live: `structure_exit.enabled` has been false since 08-24 and [`bear_arm`](arm-index.md#bear_arm)'s trigger fired again on 09-04. Rule 4 is what kills every leading candidate now, all on the same year: `be 0.40, suppressed in BEAR_HE           2024 +0.008 (n=207)  2025 +0.009 (n=161)  2026 -0.069 (n=23)`, with be 0.30/0.25/0.20 at −0.034/−0.034/−0.038 on those same 23 rows. The give-back pattern is in the **underlying**, and the days-to-peak gradient held and doubled in power: `peak within 3d               n=  39  give-back  87%  meanR -0.445` against `peak >20d                    n= 176  give-back  47%  meanR +0.236`. |
| [`next_day_move.py`](arm-index.md#next_day_move) | Move the give-back question to day 0 (knowable at the close): cut positions the stock didn't confirm? | ARM C doesn't clear the confound → **no rule**, and on 2026-09-04 the bear-keyed exception went with it: all three ARM R cuts lose their `**` and every CI straddles zero (`wrong sign +0.074 [-0.005, +0.148]`, `worse than -0.5 sigma +0.043 [-0.003, +0.090]`, `flat band +0.087 [-0.011, +0.177]`), and criterion 4 fails on the export's first 2026 rows (`-0.153` / `-0.143` / `-0.258`, n=21). Whole-book day-0 cuts still LOSE to SHIPPED. Sensitivity is structural. |
| [`volume_signal.py`](study-results/f2_management/volume_signal.md) | Share volume — the one column on disk no study had read. Does unusual-O/S (flow contracts / share volume) condition exits, or anything? | **NULL** — `VERDICT: PATH-VOL-PROXY — MFE and MAE move together with no R separation.` unchanged on 2026-09-04 (`r_sep=-0.0290`). The one frozen exit variant changes `9` rows in the whole book and none of them is in 2026, so the per-year table's `2026  n=  50  gain +0.0000` is a STRUCTURAL zero, not a null. Column closed; no version bump. Bear's monotone os_ratio read is a post-hoc carry-forward only. |
| [`exit_from_text.py`](arm-index.md#exit_from_text) | Do the model's own stated invalidation level (E1, underlying-close stop), trigger condition (E2, entry filter — a selection effect) and emitted horizon (E3, time exit) beat the shipped mechanical exits? | First run 2026-09-02 (era v4 PRIMARY): **no CANDIDATE**. The 2026-09-04 re-run (1,143 rows / 166 dates) holds it — `tally: {'UNDERPOWERED': 278, 'NOT A CRITERION (pooled)': 8, 'NULL': 22, 'CONTRARY': 8, 'SURVIVAL-ARTIFACT': 2}` — and moves two cells, both on the export's first 2026 rows. E2's pooled `ALL ALL N=3` cell (a CANDIDATE, never a criterion) is now `VERDICT: NULL` on `2026 -0.043 (n=11)`. And the queued v3 re-read is **ANSWERED**: the `bear_put_spread` E1 candidates do not reproduce on v4 — powered at `population 336 rows` and NULL at all three buffers (`2026 -0.302`, `-0.134`, `-0.039`, n=16). E1 stays CONTRARY on `bull_call_spread`/`LVOL`, now 8 cells, every criterion true toward the negative sign: the text stop cuts that structure's winners. E3 fails its survival control. |
| [`staged_exit.py`](arm-index.md#staged_exit) | Evaluate ONCE at a fixed session X on P&L vs the ORIGINAL entry — exit, tighten the stop, or arm a trail — where the reactive drawdown-from-peak rules of Attempts 1/2/10 failed. | **NULL in substance on both eras and both arms.** v3 2026-08-19 (795/118): 60/96 cells UNDERPOWERED, every powered cell failing the CI. The 2026-09-04 v4 re-run (1,143 rows / 166 dates) is the best-powered yet — `51 of 96 cells clear the floor; 45 are UNDERPOWERED.`, `tally: {'UNDERPOWERED': 45, '-': 51}` — and not one powered cell reaches CANDIDATE or REACTIVE-AGAIN (a bare `-` is a cell that failed criterion 1; `NULL` is reserved for a cell that clears the CI and then fails stability, and there are none of those). The extra power bought the opposite of a candidate: SIX cells now have a CI excluding zero and all six are HARMFUL, e.g. ARM E X=20 R ≥ +0.25 at `DeltaR  -0.033` / `[-0.054, -0.011]`. The Attempt-1/2/10 null extends to scheduled switches, now with a measured cost. |
| [`exit_drawdown.py`](arm-index.md#exit_drawdown) | Does any exit rule — chosen WITHOUT look-ahead, on TRAIN dates only — reduce the ACCOUNT-LEVEL mark-to-market drawdown of the deployed [`account_sim`](arm-index.md#account_sim) book without giving back its edge? Five arms: W (walk-forward selection over the shipped pt×sl×tef grid, the honesty baseline), U (underlying ATR stop, ATR frozen at entry), O (flow-unwind off the entry long leg's own Open Int path, read lagged one session, plus one volume-climax variant), P (partial scale-out, exact), and D (a SECONDARY drawdown THROTTLE on sizing, which can never ship). Every standing exit null in the record was reached on a per-row R estimand under in-sample parameter choice; this is a path-dependent marked dollar drawdown chosen out of sample. | Registered, built, run and **graded 2026-09-05** (`research/pre-registrations/f2_management/exit_drawdown.md`, four dated wording corrections). The registration named the modal expected outcome in advance and got it: **every PRIMARY (dense-episode) cell is UNDERPOWERED** — `tally: {'UNDERPOWERED': 7, 'SECONDARY-UNDERPOWERED': 1}`, `PROD-ROBUST` NOT claimed — because the purged walk-forward leaves only 43 OOS dates / 296 rows behind a 45-date burn-in, short of the pre-registered 25-date / 60-row floor at every arm. On the disclosed `all` cut (91 OOS dates / 607 rows, **no verdict read from it**) exactly two cells clear power and both are negative: `ARM O/vol` NULL (max DD $-14,238 → $-7,194 but CI95 [-268, +13,843], ΔR -0.054 [-0.130, +0.017], 71% continuation sales — the reactive null of Attempts 1/2/10 and `staged_exit`, measured at account level for the first time) and `ARM D/throttle` SECONDARY-NULL (real $-379 vs tweak $+2,616). v3 has no surviving test block on its PRIMARY population. The durable finding is that the in-family best DRIFTS block to block, and the in-sample disclosure gap sizes the tuning bias every earlier in-sample exit read carried. Nothing ships; a candidate would still need an independent window. |

<a id="structure"></a>
## ③ STRUCTURE — "am I expressing the signal in the wrong wrapper?"

| File | The question | Verdict |
|---|---|---|
| [`bear_rewrap.py`](arm-index.md#bear_rewrap) | A bear *spread* sells the lower put — so it gives away the vol expansion that makes a bear position pay. Drop the short leg? | Wrapper is worth **+0.085** and it **does not hold in 2026** — and on the 2026-09-04 re-run (after the `entry_price_of` fix) that is now a tested statement rather than a vacuous one, because this is the first bear_rewrap book that CONTAINS 2026 dates (`bear debit rows: 391`, `reconstructed                391  (100.0%)`). The naive re-wraps stay 0 of 5 (`long_put` `dR -0.042`, `wider` `dR -0.084`). The DIAGONAL is 4 of 5 and loses exactly the criterion that could not be tested before: `[FAIL] sign-stable every year    2024 +0.195  2025 +0.259  2026 -0.106`, against `dR +0.205 CI [+0.059, +0.360]`, `MIN +0.171 over 119 folds`, both ex-window cuts and both pricing tiers. In the SAME run its portfolio checks are MET for the first time (`P1 worst-decile: n= 16  meanR +0.499  CI [+0.202, +0.743]`, `P2 correlation with deployed sleeve: -0.326`). The candidate LOST its year criterion and GAINED its portfolio checks in one run — neither a ship nor a refutation. Note which check tests the new year: the YEAR clause does, the window cut does not (`ex_2026_feb_apr` drops 1 long_diag row of 189), and the 2026 leg is 79 rows of the pooled book. Nothing ships. |
| [`vol_sleeve.py`](study-results/f3_structure/vol_sleeve.md) | Synthesize straddle / strangle / calendar on the dates the engine already signalled. Is there a vol sleeve? | **CLOSED** — the straddle clears its gate then dies out of sample, and it *doubles* existing exposure (wrong-signed correlation). Only the **calendar** survives. The 2026-09-04 re-run says the same in the same shape: `Q1 non-null: True   Q2 non-null: False` and `Q2 IS NULL — the sleeve is neither reliably anti-correlated with the deployed book nor reliably positive on its worst dates.` The sign check is the whole argument — straddle `+0.220   CI95 [+0.063, +0.384]` and strangle `+0.187   CI95 [+0.036, +0.354]` against calendar `-0.211   CI95 [-0.384, -0.026]`: same signs as 08-24, all three weaker. Which cells clear Q1 moved with the new rows (`Q1 NON-NULL cells: straddle/>90, strangle/>90, calendar/ALL, calendar/>90`, where 08-24 had straddle/ALL and no strangle) — composition, not a finding. The calendar's numbers are printed POST-HOC, not as the gate (`n= 133  win   57%  PF  1.39  meanR +0.303`), and the two structures split on the new year (straddle `2026:-0.31`, calendar `2026:+0.67`, on 61 rows / 11 dates). They go on to [`calendar_hedge`](arm-index.md#calendar_hedge). |
| `calendar_hedge.py` | Re-derive that one survivor under a pre-registered pick rule and a strict fill rule. | Gates pass and the primary stays unreadable. The 2026-09-04 re-run first had to earn its gate back: R2 now reads `reconstructs: 1122 / 1122  (100.0%)` → `R2 PASS`, where the evening's FIRST suite pass stopped at 1121/1122 on one `entry_unpriced` row (UTHR 2025-12-17) — `bear_rewrap.entry_price_of` had no branch for an entry day with no open and no mark, and now carries a prior mark forward, mirroring production's third branch. R1 `hard=0`, R3 `deployed: 362 positions over 147 dates, $74,001   meanR +0.216`, `R4 PASS`. The VERDICT block is unchanged (`H0 FILL           NOT MET`, `H2 (primary)      NOT EVALUABLE`, `H2 under hold     NOT EVALUABLE`) and the fill rate still binds on a book twice the size: `75 / 147  =  51.0%` deployed, `4 / 14   =  28.6%` worst-decile. H2(a) `-0.100  CI95 [-0.228, +0.017]`; H2(b) `n=4` → NOT EVALUABLE; H2(c) `tail positive in 3/3 evaluable years` but carried on the new year by ONE position over two dates. **H3 is an unstable measurement, not a verdict**: `NOT MET at any size` on both baselines now (`bound by: drawdown fails; worst-date ok at every f`, baseline max DD −12,529 and −15,425 against −10,968 and −11,467 on 08-27) — NOT MET / DEPLOYABLE / NOT MET across three consecutive exports, so carry none of the three as evidence. H4 went flat (`n=75 dates  dR +0.001  CI [-0.071, +0.073]`). H5 is POST-HOC and its BEAR_HE cell is degenerate at n=1. Blocked on dates, not refuted. |
| [`financed_spread.py`](arm-index.md#financed_spread) | Does wrapping a book debit vertical in a financing credit — an opposite-delta credit spread, a naked short leg, or a same-direction credit vertical — improve outcomes without re-wrapping the same exposure? | Two runs 2026-08-19 (era v3). Same-expiry shapes: all seven cells **NULL**, naked short significantly harmful, every shape re-wraps the sleeve. On the 2026-09-04 v4 re-run (`book 1143 rows / 166 dates`, `kept 797  (bull 414 / bear 383)`) the study prints its **RE-WRAP** token for the first time: `F3 off1          RE-WRAP` — the same-direction financed vertical clears six of seven (`dR +0.217  CI [+0.056, +0.401]`, `MIN +0.173 over 124 folds`, all three window cuts, every year incl. `2026 +0.353`, both tiers, `124 dates`) and fails only `[FAIL] 7 E3 <= 0 (does not re-wrap the sleeve)  corr +0.159 over 112 shared dates`, which is the token's registered meaning: a real gain that is the same exposure again. Its own sizing control does not separate (`FIXED-CONTRACTS control` `n= 233   dR +0.102   CI [-0.102, +0.301]`). The v3 **F4-d20 hold** candidate goes unconfirmed a THIRD time — `UNDERPOWERED` at `n=36 rows / 33 dates`, `target_unreachable=179`. Everything else NULL, including `F2 off1` at `dR -0.497  CI [-1.084, -0.085]`, harmful but labelled NULL because only a positive cell can be a candidate here. NOT a ship. |

<a id="deployment"></a>
## ④ DEPLOYMENT — "can I actually run this?"

| File | The question | Verdict |
|---|---|---|
| [`bear_deploy.py`](arm-index.md#bear_deploy) | Bear selection is unfixable — but is bear worth holding as a **hedge**? Four estimands: D1 joint selection×exit, D2 hedge contribution, D3 sizing, D4 conditional pick. | Bear = a hedge, not a selection — that half is unmoved (D1 NOT MET on every run). The hedge case REVERSED on the 2026-08-24 v4 export and stays reversed on 2026-09-04: **D1–D4 all NOT MET**. D2 fails on the year check alone, and the check is a genuine three-year one now — the hedge conditions pass (`+0.032`, n=37; `sleeve correlation -0.141`) and then `tail positive in 1/3 evaluable years — needs >= 2: NO`, the one positive year being 2026 at `n=  2`. D4: `rankers tested: 10  adopted: 0`, the best being `\|delta\| low first` at `+0.084 [-0.028, +0.197]` — a CLOSER-to-money pick with a CI spanning zero, so the [§4](../docs/deployment-rules.md#s4) far-OTM prohibition is still not contradicted. D5 (POST-HOC) narrowed from eight candidate gates to **two**, the same cell at two sizes (`mech vol H-VOL f=1.00` and `f=0.50`). The §4 pick line stays **PULLED** and the sleeve is **operator policy** ([`deployment-rules.md` §4](../docs/deployment-rules.md#s4)); the v3 D2 MET / D4 ADOPTED read is in [`deployment-evidence.md`](deployment-evidence.md). |
| [`account_sim.py`](arm-index.md#account_sim) | The ladder assumes infinite capital. Does a real **$25,000** account — paying for positions, holding reserve, respecting a delta cap — still produce a book? | Caps survive; the **window** doesn't. **Delta-notional binds, not cash.** Feasibility only, nothing ships. `>>> FEASIBLE <<<` still holds on the PRIMARY dense episodes (2026-09-04: `3 episodes, 88 dates`, headline cell `n=148  dates=76  $22,217  meanR +0.348`, `maxDD $-3,750 = 15.0% of capital`, G2–G5 all PASS). The first 2026 dates put a boundary on that: the SECONDARY full book (`n=260  dates=129  $21,855  meanR +0.231`) now fails A1 on `2026:-0.062` and A3 on `maxDD $-8,920 = 35.7% of capital`. No verdict reads from the secondary population — but FEASIBLE is explicitly a TWO-YEAR, DENSE-EPISODE claim, because the episodes stop at `2025-09-26` and nothing in the FEASIBLE read has seen 2026. The compounding arm is `NOT FEASIBLE AT $25,000 — BLOWUP RISK (A1 holds, A3 fails)` at `25.7% of capital`. |
| [`selection_order.py`](arm-index.md#selection_order) | `account_sim`'s *rejected* picks outperformed its taken ones. Does a different **blind entry-side order** of the same candidate set spend the scarce delta budget better — or was that read an artifact? | **ORDERING-IS-NOISE** (v4; re-run 2026-09-04) — thread CLOSED. All four arms clear G0 on the PRIMARY population (O1 30 / O2 27 / O3 32 / O1b 32 affected dates against the pre-declared floor of 25), and none clears all seven parts: every arm sits inside the O4 random-order band (`p95 +0.0524`, best arm at `pct 76%`). The adverse-ordering read was an ARTIFACT of which picks the cap happened to exclude. The first 2026 dates change nothing and cannot: PRIMARY spans two calendar years so its criterion-4 line has no 2026 term at all, and on SECONDARY 2026 is `(n=3)` dates for every arm, which fails criterion 4 with or without it. Earlier runs (v3, and v4 through 08-24) were UNDERPOWERED at G0; the `7–14%` figure those quoted was a hardcoded prose literal, corrected 2026-08-22. |
| [`hedge_timing.py`](arm-index.md#hedge_timing) | The bear hedge sleeve is deployed on discretionary triggers — chop, a SPY gap-up, a 4–5-day SPY down-run. Does any of them, made mechanical, pick a day the hedge beats the **same day's** ladder-eligible long? | **RUN 2026-09-04** (era v4 sha e59356f; re-graded after the 08-28 run). **TIMING-CANDIDATE survivors: 0 of 9** (~0.45 expected by chance) — unchanged — but WHICH arm carries the gap-up finding MOVED, so quote the current one. `hedge_timing ARM H1-GAP` went NULL → `VERDICT H1-GAP: CONTRARY` (`delta -0.245`, `CI95 (date-clustered, between) [-0.458, -0.026]`); the PRIMARY within-date arm is CONTRARY and stronger, `HEADLINE difference -0.506  CI95 [-0.844, -0.157]`; and the dollars arm went the other way, `VERDICT H4-GAP: NULL` on `cuts_ok=F`. So the drafted §4 prohibition (*do not open the hedge on a gap-up day*) now rests on **H3-GAP**, with H1-GAP mirroring it between dates and H4 no longer carrying it; it stays **HELD for the operator** per the registration. CHOP and DECLINE-BROAD are now `NULL` on the money arm where 08-28 read them UNSTABLE. The operator's own 4–5-day streak stays **UNDERPOWERED** exactly as fixed in advance (`N=4` `n_dates=4`, `N=5` `n_dates=2`; no direction is ever quoted from those). **Nothing ships**; forward trigger unchanged: ≥25 strict-streak dates or ≥25 post-2025-11-04 dates. |
| [`hedge_exposure.py`](arm-index.md#hedge_exposure) | When the open book is **concentrated** in one correlated cluster, does a long put on that cluster's proxy cut the book's **mark-to-market** drawdown, versus carrying the same concentrated book unhedged? | **UNDERPOWERED (the mechanism question) + MEASUREMENT-ONLY (ARM M)**, both unchanged on 2026-09-04. Two words over two different objects; the registration defines both and orders neither, so both are emitted. The **ERRATUM 1** population deadlock was **ratified by the operator on 2026-08-31** (consolidated into [`pre-registrations/f4_deployment/hedge_exposure.md`](pre-registrations/f4_deployment/hedge_exposure.md) §Population and basis): the population is the literal `load_book(include_bs=False)` call, because a `strike_expiry_tweak` row is a **real** Barchart price for a nearby strike and an operator who does not follow a proposed leg exactly is modelled better by a book that admits the substitution. `real` is kept as a **reported stratum**, never a co-primary. On the ratified population (`1143 rows / 166 signal dates`) it still reads `powered POOLED cells 0   POOLED cell words: UNDERPOWERED 9`, hence `VERDICT — the mechanism question, over the hedge cells: UNDERPOWERED`, and **no direction is quoted from any cell**. NEW and reported only: the `real` stratum crossed its power floor for the first time — `535 rows / 158 signal dates`, `powered POOLED cells 9   POOLED cell words: NULL 9` — the first hedge-cell reading this study has ever powered, and it is a NULL, on a stratum the registration forbids reading a verdict from. ARM M is not power-gated and is the sharper result: `ARM M curve gap: maxDD $-9,332   ulcer +2.81 pts   TUW +2.1 pts   (differ materially: YES)` — `the close-bucketed curve UNDERSTATES this book's max drawdown by 40.2%.`, hence `VERDICT — ARM M, the measurement, which is not power-gated: MEASUREMENT-ONLY`. **Nothing ships.** UNDERPOWERED leaves the queued max-drawdown question **open**, it does not close it. `bear_deploy` D3, [`calendar_hedge`](arm-index.md#calendar_hedge) H3 and `hedge_timing` H4 all **stand** — but they were read on the close-bucketed curve, now a known limitation of theirs. **ERRATUM 2** stands: `hedge_exposure ARM P` is **inert as registered** and has not been redefined, so the binding prose rule is unreachable; `hedge_exposure ARM RF` prints as **UNREGISTERED — ADDED AFTER COMMIT** and no clause reads it. Read with the ratification's own limitation: the registration's **plan-time observations** describe the `real` stratum and are **not** disclosures about the ratified book. |
| [`hedge_concentration.py`](arm-index.md#hedge_concentration) | On the **admitted** book — the positions `account_sim` actually takes under the operator's top-3-per-day rule and exposure caps — does a session's cluster **concentration predict** the book's subsequent mark-to-market drawdown, and only then does a proxy put on that cluster cut it? | **PRECONDITION-NULL — a POWERED null**, which is the whole point of the two-stage design: `hedge_exposure` could not power one hedge cell, and Stage 1 does not depend on triggers at all. Unchanged on the 2026-09-04 re-run: `usable sessions per concentration tercile   [216, 215, 195]   floor 60 EACH   PASS` / `dense episodes of admitted signal dates     3   floor 3   PASS` / `G-POWER-K: PASS`, then `contrast $-173.65  CI95 [$-1,205.66, $893.81]` and `rho +0.0000  CI95 [-0.2198, +0.2231]` — the point estimate is now AT zero — with the contrast well inside the circular-shift null (`null p05 -635.5211 ... beats p05 (more negative): no`). Clauses 1, 2, 3 and 5 fail; the two that PASS are the **controls** (sign kept in 2 of 3 gross terciles, and under both ex-window cuts), so it is not a gross-exposure effect in disguise either — it is no effect. The second control is real for the first time: `ex_2026_feb_apr    rows  584  usable  564  contrast       $-164.28   sign kept` is a genuine cut now, not the no-op it was on a book ending in 2025. `VERDICT — Stage 1 (ARM K, the precondition): PRECONDITION-NULL`, so `VERDICT — Stage 2 (ARM C, the mechanism): NOT RUN (Stage 1 PRECONDITION-NULL)` and **no cell was evaluated** (its census is on the record: episodes peak at 18 against a floor of 25, as the registration predicted). Book: `candidate rows (ratified population)          1143   / 166 signal dates` → 513 ladder-eligible → `ADMITTED (taken + taken_downsized)             260   / 129 dates`, skipped per_pos_delta 101 · net_delta 84 · day3_cap 68, partition EXACT. G-ADMIT, G-MTM (`positions 260   reconciled 260 ... worst mismatch $0.0000`) and G-BLIND all pass. ARM M is a measurement and never a verdict here: `THE GAP ... maxDD $-5,318 (59.6% of the realized-on-close drawdown)   ulcer +3.71 pts   TUW +11.4 pts`. **Nothing ships.** `SHIP-CRITERIA BRANCH: record in research/deployment-evidence.md as closing the queued max-drawdown question for concentration-gated hedging; next-steps.md §2.1 closed` — the operator's to write, not this study's. Says nothing about hedging in general and does not touch the §4 sleeve. |
| [`concurrency_correlation.py`](arm-index.md#concurrency_correlation) | `max_positions_per_day` caps the FLOW of new positions; nothing caps the **STOCK** of open ones. Does the SIZE and internal SIMILARITY of the open book degrade per-position outcome, independently of what was selected? | **NOISE** (first run 2026-09-04, era v4; numbers from that evening's re-run on the refreshed export): `all 11 powered arms sit inside ARM N's band. Neither the SIZE of the open book nor its internal similarity degrades per-position outcome on this era's deployed book, at any ceiling on either grid.` PRIMARY `3 episodes, 88 dates` / `positions 221`; SECONDARY `147 dates` / `positions 362`. On the bigger book `C ceiling 8` and `C ceiling 20` LOSE the one criterion they held (`criteria met ----`, both `--6-` on the preceding run), so no ceiling arm holds anything. `K 3` and `K 5 / same-underlying` are UNDERPOWERED (21 and 7 moved dates against the pre-declared floor of 25) and print census only; **ARM CK NOT RUN** — neither component clears alone. Three things the run established rather than assumed: the book is **long-only** (`{1: 362}`, 362/362 with same-direction count == open count), so `ARM K / same-direction` **IS ARM C** on a different grid and is excluded from the conjunction; **X7's delta control barely discriminates** (121 of 147 dates in `[2.0,inf)`, leaving ONE readable band, and one band is the whole sample re-labelled, not a control); and ARM D0's DESCRIPTIVE shape is **not flat** (`[0,3)` +0.4317 → `[6,10)` −0.0305 by same-direction-and-sector count) but is registered descriptive-only and every ceiling arm that would act on it sits inside the null band. **X4 is now SETTLED** — by hand, from two reports, as the registration intends (`X4 is settled by reading the two reports side by side: same sign, both clearing X2 and X3, and point estimates within 0.15 R. Until that is done, no arm from this study is ADOPT-eligible.`; the runs themselves still print `X4 ERA STABILITY: PENDING` on every arm, because `lib/era.py` binds one run to one era). The `--era v3` companion ran the same evening and is recorded in [`study-results/f4_deployment/concurrency_correlation.md`](study-results/f4_deployment/concurrency_correlation.md): on the v3 era book (795 rows / 118 dates), `arms run (PRIMARY): 13   powered past X1: 8   clearing X2/X3/X6/X7: 0` — NOISE there too. So: no arm clears X2/X3 in EITHER era, nothing is or can be ADOPT-eligible, and the VERDICT is era-stable while the per-arm GAINS are not (of the 8 arms powered in both eras, 4 keep sign and 4 flip, two of them 0.155 R apart — outside the rule's 0.15 R band). Thread CLOSED on both eras; nothing ships. |
| [`portfolio_delta.py`](arm-index.md#portfolio_delta) | Is the book's net delta LEVEL a lever? Dose-response by exposure band, a ceiling-band admission, and a delta-TARGETED hedge sleeve, all against a seeded random-admission null band. | **CANDIDATE-FOR-INDEPENDENT-WINDOW**, the label unchanged from 2026-08-27 and re-earned on the 2026-09-04 v4 run (`deployed picks 362 over 147 dates`) — but the survivor set **HALVED**. Only `B ceiling 1.00` clears the full seven-part conjunction (`paired mean gain +0.1081 R   CI95 [+0.0131, +0.2205]`). `B ceiling 1.50`, which cleared on both populations on 08-27, now fails on both: PRIMARY at criterion 1 (`+0.0681 R   CI95 [-0.0120, +0.1527]`) and SECONDARY at criterion 4 on the export's first 2026 rows (`2026 -0.0878 (n=11)`). ARM D still reads `SHAPE: NON-MONOTONE / FLAT`. **NOTHING SHIPS** and no ceiling value may be adopted on its P&L — the label queues an INDEPENDENT WINDOW and nothing else. |

---

<a id="infrastructure"></a>
## Infrastructure (not studies — `run.py` lists these in `INFRA`)

| File | Role |
|---|---|
| `run.py` | The runner. Writes `backtests/study_output/<name>-latest.txt` with a provenance header (git sha, dirty flag, exact argv, input row counts + mtimes) so no write-up can attribute numbers to the wrong export. |
| `harness.py` | **FROZEN** `Trade`/`replay`. Prices nothing — it replays a stored mark series. Every recorded conclusion depends on its exact clamps and rounding. Changing the exit mechanism means *copying* this file, never editing it. |
| `book.py` | Pooled real + proxy loader. `bs_options_hist` rows are excluded by default — they're priced *from* the model that scores them. |
| `protocol.py` | The four things every conclusion rests on: date clustering, purging + embargo, same-dates comparison, window dominance re-cuts. Plus the profit-factor helpers (`pf`, `pf_ci_by_date`, `pf_paired_by_date`) — never printed without mean R beside them. |
| `text_corpus.py` | The model's own prose (thesis, Alt, signal items, trigger, invalidation) joined to every priced row through `book.py`'s join, parsed back into the writer's fields, and reduced to the regex features with no numeric twin already tested null. Also returns the *unpriced* analysis rows by reason and a citation check against the raw flow feed. Built for `text_features`, `exit_from_text`, `prompt_eval`. |
| `underlying.py` | Daily stock bars — real OHLC, falling back to close-only `Price~`. The widening `harness.py` is frozen out of. |
| `underlying_features.py` | As-of-entry price-*state* columns (rv20, Parkinson, semivar, ATR%, efficiency ratio, VRP, beta). This family is the ML re-open — none of it existed when B1 searched 496 subsets. |
| `volume_features.py` | As-of-entry *volume* columns (unusual-O/S, relative-volume z, Amihud), split-guarded, rescaled tickers withheld from the window features. Built for `volume_signal` (NULL), kept for future pre-registered use. |
| `macro_calendar.py` | Scheduled macro events (FOMC, minutes, CPI, NFP, PCE) as as-of features, read from the hand-authored `config/macro-events.yml`. `next_event` is strictly-after and refuses past each type's `verified_through`; unscheduled events excluded from forward reads only. Built for `macro_event_study`. |
| `greeks.py` | Per-leg greeks from the option-history cache at a given day, signed and qty-scaled; net sums are all-or-nothing per greek (a missing leg makes the greek `None`, never 0). Built for `financed_spread`'s exposure reads and `portfolio_delta`'s G-DELTA cross-check. |

---

## How to read any one report

1. **Check the header.** Row counts and mtimes of the input exports. Two runs on different exports are not comparable. That has caused a wrong attribution before.
2. **Check the calibration gate.** Most studies open by proving production rules reproduce the stored `exit_reason` / `days_held` / `realized_pnl_pct`. A non-zero exit here is the gate *working*. Do not route around it.
3. **Check the pre-registration.** Nearly every study names a `current.md` section written *before* it ran. If a number isn't covered by a pre-registered criterion, it's an observation, not a result.
4. **Metric definitions** live in [`glossary.md`](glossary.md). `E` = P&L at path cap (selection only). `R` = realized under the exit rules (selection + exit). `E<0` means no exit rule can rescue it.

## Recurring traps this log has actually fallen into

- **Composition, not signal.** A cut looks predictive because it changed the *mix* of structures, not because the variable matters. Killed `oi_confirm_pct`, `iv_pct`, and the `score_total` bands.
- **Grading against a baseline production doesn't run.** Changed a decision twice. Always compare against the *shipped merge*, not against a clean default.
- **One window carrying an effect.** Every headline is re-cut ex-Mar–Apr-2025 and ex-Feb–Apr-2026.
- **Row count ≠ sample size.** Rows inside a signal date share the tape. `n` is ~118.
